{application, foo,
    [
        {description, "test"},
        {vsn, "1.0"},
        {modules, [
            foo_app,
            foo_sup,
            foo_server
        ]},
        {registered, []},
        {applications, [kernel, stdlib, lager]},
        {mod, {foo_app, []}},
        {env, []}
    ]
}.
